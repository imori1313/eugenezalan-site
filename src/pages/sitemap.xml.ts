import {projects} from '../data/projects';
export function GET(){const paths=['/','/projects','/about','/commission','/contact','/privacy',...projects.map(p=>`/projects/${p.slug}`)];return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${paths.map(p=>`<url><loc>https://eugenezalan.com${p}</loc></url>`).join('')}</urlset>`,{headers:{'Content-Type':'application/xml'}});}
